
def derivative(f,x,h=0.00001):#using limits to evaluete the dev func
    
    return (f(x+h)-f(x))/h

def f(x):
    return x**2#function

    #need to make the function dynamic in future projects as poly then use tri calcules to make fully use of dev
